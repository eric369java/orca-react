export const dateToFormattedString = (date: Date) => {
    // Pass in local representation of date.
    return `${date.getFullYear()}-${date.getMonth()}-${date.getDate()}`;
}

export const formattedStringToDate = (date: string) => {

}
//2025-06-13